return {
  defaults = require("nvchad.configs.nvimtree").defaults,
  git = {
    enable = true,
  },

  renderer = {
    highlight_git = true,
    icons = {
      show = {
        git = true,
      },
    },
  },
}
